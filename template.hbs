var __dirname = '{{__dirname}}';
var __filename = '{{__filename}}';
function require (thing) {
  var mod = require.map.get(thing);
  if (mod && 'default' in mod) {
    return mod.default;
  } else {
    return mod;
  }
}
require.map = new Map();
{{#each require}}
  module ___id_{{name}} from '{{path}}';
  require.map.set('{{path}}', ___id_{{name}});
{{/each}}
{{#each core}}
  require.map.set('{{name}}', _require('{{name}}'));
{{/each}}
var module = {};
var exports = module.exports = {};

{{{code}}}

export default module.exports;