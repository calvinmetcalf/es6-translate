{{#each require}}
  import '{{.}}';
{{/each}}
require.__self = this;
function require(thing) {
  var mod = require.__self.get(thing);
  if ('default' in mod) {
    return mod.default;
  } else {
    return mod;
  }
}
var module = {};
var exports = module.exports = {};

{{{code}}}

export default module.exports;